const ProductDetailsLayout = ({ children }: { children: React.ReactNode }) => {
    return (
        <>
            {children}
            <h1> Product details featured layout </h1>
        </>
    )
}

export default ProductDetailsLayout;